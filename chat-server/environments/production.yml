service:
  targetPort: 3000

env:
  PORT: 3000
  MONGODB_DB_NAME: docs_chat_prod

# TODO: add this via kubectl when database is created
# envSecrets:
#   MONGODB_CONNECTION_URI: chat-server-atlas-uri-prod

ingress:
  enabled: true
  hosts:
    - chat-server.docs.prod.corp.mongodb.com

resources:
  limits:
    memory: 1300Mi
  requests:
    memory: 1100Mi
