image:
  repository: node
  tag: 18

cronJobs:
  - name: eval-quality
    schedule: "0 4 * * *" # every day at 4am UTC (11PM EST)
    command: ["npm", "run", "eval:conversationQualityCheckPipeline"]
    env:
      MONGODB_DATABASE_NAME: docs-chatbot-prod
      CONVERSATIONS_SERVER_BASE_URL: "https://chat-server.docs.staging.corp.mongodb.com/api/v1"
    envSecrets:
      MONGODB_CONNECTION_URI: docs-chatbot-prod
      OPENAI_ENDPOINT: docs-chatbot-prod
      OPENAI_API_KEY: docs-chatbot-prod
      OPENAI_CHAT_COMPLETION_DEPLOYMENT: docs-chatbot-prod

    resources:
      # guaranteed amount of resources
      requests:
        cpu: 100m
        memory: 2Gi
      # maximum allowed resources
      limits:
        cpu: 500m
        memory: 5Gi

# Alerts
defaultAlerts:
  enabled: true
  email: "chatbot-alerts@mongodb.com"
